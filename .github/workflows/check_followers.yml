name: Instagram Takipçi Kontrolü

on:
  schedule:
    # Her 3 saatte bir otomatik çalışır (00:00, 03:00, 06:00, 09:00...)
    - cron: '0 */3 * * *'
  workflow_dispatch: # İstediğinde elle çalıştırma butonu

jobs:
  check-followers:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write # Takipçi dosyasını repoya kaydetmek için gerekli izin

    steps:
    - name: Repoyu Klonla
      uses: actions/checkout@v4

    - name: Python Kurulumu
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Bağımlılıkları Yükle
      run: |
        python -m pip install --upgrade pip
        pip install requests instaloader

    - name: Kontrol Kodunu Çalıştır
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        IG_USERNAME: ${{ secrets.IG_USERNAME }}
        RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
      run: python main.py

    - name: Değişiklikleri Repoya Commit Et
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        
        if [ -f follower_count.txt ]; then
          git add follower_count.txt
          git commit -m "Takipçi sayısı güncellendi [skip ci]" || echo "Değişiklik yok, commit atlandı."
          git push
        else
          echo "follower_count.txt bulunamadı."
        fi
